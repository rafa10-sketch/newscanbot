# ============================================================
# PentestBot v2 — Python Dependencies
# Install: pip install -r requirements.txt
# ============================================================

# ── Web API Framework ─────────────────────────────────────────
fastapi>=0.115.0
uvicorn[standard]>=0.32.0

# ── Telegram Bot Framework (optional — only needed for Telegram mode) ──
python-telegram-bot[job-queue]==21.5

# ── Async SQLite ──────────────────────────────────────────────
aiosqlite>=0.20.0

# ── Internal HTTP client (for Groq API + async calls) ────────
httpx>=0.27.0
aiohttp>=3.10.0

# ── Secondary TLS validation ─────────────────────────────────
sslyze>=6.2.0

# ── PDF Generation ───────────────────────────────────────────
reportlab>=4.2.0

# ── Environment variable loading ─────────────────────────────
python-dotenv>=1.0.0

# ── Async utilities ──────────────────────────────────────────
anyio>=4.3.0

# ── Security tool Python wrappers ────────────────────────────
wafw00f>=2.2.0
s3scanner>=2.0.2
defusedcsv>=2.0.0
